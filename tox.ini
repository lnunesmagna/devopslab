[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    coverage
commands =
    #coverage run -m pytest
    coverage run -m pytest --source=.
    coverage xml
 
[coverage:run]
relative_files = True
#source = /
branch = True